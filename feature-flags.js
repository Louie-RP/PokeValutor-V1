/* Merge this into your existing public frontend configuration. */
window.PV_FEATURES = Object.assign({}, window.PV_FEATURES, {
    // Set false to hide the component immediately without editing card.js.
    priceHistory: true,
});
